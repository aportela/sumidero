var profile = (function () {
    "use strict";

    var template = function () {
        return `
    `;
    };

    /* signIn component */
    var module = Vue.component('sumidero-profile-component', {
        template: template(),
        data: function () {
            return ({
            });
        }
    });

    return (module);
})();